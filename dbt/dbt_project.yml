name: knowledge_base
version: '1.0.0'
config-version: 2

profile: knowledge_base

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  knowledge_base:
    staging:
      +materialized: view
    mart:
      +materialized: table
